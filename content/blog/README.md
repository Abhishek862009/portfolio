# Blog content

MDX/Markdown posts go here once ready. Each file should include frontmatter
(title, date, summary, slug). Wiring these files into `/blog/[slug]` (reading
via `gray-matter` + `next-mdx-remote`) is a follow-up phase — not part of the
initial scaffold, per the locked decision not to publish placeholder content.
