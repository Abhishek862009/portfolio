# Images

Place the final hero photo here as `hero-photo.png` — transparent background,
waist-up, hands folded, per the locked Hero spec.

That's the only step. `app/page.tsx` checks for this exact file and Hero
automatically renders it instead of the placeholder — no code changes
needed.
